# Core Discord
discord.py[voice]>=2.4.0
discord-ext-voice-recv>=0.5.0
PyNaCl>=1.5.0

# Music
wavelink>=3.5.0

# HTTP / API
aiohttp>=3.9.0
openai>=1.0.0

# Database — PostgreSQL only. The bot no longer uses SQLite.
asyncpg>=0.29.0

# Media / documents
Pillow>=10.0.0
pypdf>=5.0.0

# Runtime
python-dotenv>=1.0.0
psutil>=5.9.0
colorlog>=6.0.0

# NOTE: ffmpeg is a system binary installed by nixpacks.toml, not a pip package.
# The PyPI project named "ffmpeg" is an abandoned stub and was removed from here.
